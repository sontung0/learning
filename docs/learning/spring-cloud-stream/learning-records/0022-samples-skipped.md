# Samples (Skipped)

The user elected to skip the Samples lesson. The official documentation page for Samples is a single line pointing to the [spring-cloud-stream-samples](https://github.com/spring-cloud/spring-cloud-stream-samples) GitHub repository rather than containing conceptual reference material. The user reviewed the repository's sample list (batch producer/consumer, multi-binder, multi-function, partitioning, routing, schema registry, kafka-streams, kafka-security, transaction-kafka, testing, native-app, etc.) and will refer back to it as a practical reference when implementing real features rather than treating it as a lesson.

**Evidence:** User explicitly skipped this topic; it is a links page, not a teaching unit. Sample repo bookmarked for practical reference.