# Configuration scopes and binding contracts

The user demonstrated how to configure functional bindings by name, distinguish destinations, consumer groups, and consumer concurrency, define shared stream defaults, override a selected binding, and choose between the application default binder and a per-binding binder. The user also recognized that Kafka-only properties must remain in the Kafka binder namespace rather than the portable binding namespace.

**Evidence:** The user completed the Lesson 0016 configuration review drill with correct explanations of binding names, configuration scopes, and binder selection.